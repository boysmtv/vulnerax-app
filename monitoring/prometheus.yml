global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "D:\Work\Pribadi\WebProject\vulnerax-app\monitoring"
    static_configs:
      - targets: ["localhost:9090"]
    metrics_path: /metrics

  - job_name: "otel-collector"
    static_configs:
      - targets: ["localhost:8889"]
